## Fraction Program

Dustin Horvath

#### Parses text input files into fraction operations and displays the output. Catches non-integer input and divide-by-zero attempts.

To use, `make`, then `./test`. It will ask for input filename before
defaulting to "input.txt".
